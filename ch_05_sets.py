# sets are unordered
# sets are unindexed cannot access through indexes
# there is no way of changing the set values
a={1,1,2,3}
print(a)
print(type(a))
# sets should not allow repetative values or duplicative values

# empty set
a={}
print(type(a)) #this syntax will create an empty dictionary but not empty set
# an empty set can be created by 
a=set()
print(type(a))